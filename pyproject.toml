[project]
name = "wander-tools"
version = "0.1.0"
description = "Local authoring and validation tools for Wander"
requires-python = ">=3.11,<3.13"
dependencies = [
    "alembic>=1.16,<2",
    "asyncpg>=0.30,<1",
    "boto3>=1.40,<2",
    "certifi",
    "fastapi>=0.118,<1",
    "httpx>=0.28,<1",
    "modal>=1.1",
    "pydantic>=2.11,<3",
    "psycopg[binary]>=3.2,<4",
    "python-multipart>=0.0.20,<1",
    "numpy>=1.26,<2",
    "scipy",
    "pillow",
    "opencv-python-headless<4.12",
    "matplotlib",
    "numba",
    "plyfile",
    "sqlalchemy>=2.0,<3",
    "temporalio>=1.18,<2",
    "trimesh",
    "uvicorn>=0.37,<1",
]

[dependency-groups]
dev = ["ruff==0.16.8"]
orchestrator = [
    "alembic>=1.16,<2",
    "boto3>=1.40,<2",
    "fastapi>=0.118,<1",
    "httpx>=0.28,<1",
    "pydantic>=2.11,<3",
    "psycopg[binary]>=3.2,<4",
    "python-multipart>=0.0.20,<1",
    "sqlalchemy>=2.0,<3",
    "temporalio>=1.18,<2",
    "uvicorn>=0.37,<1",
]
# Local masks and pose-artifact readers; no LHM/Pi3/gsplat GPU worker environment.
inference = [
    "torch>=2.6,<3",
    "torchvision>=0.21,<1",
    "transformers>=4.46,<5",
    "huggingface-hub",
    "safetensors",
]

[tool.uv]
package = false

# Linux/Windows local tools use CPU wheels. macOS uses PyPI's native wheels (MPS capable).
[tool.uv.sources]
torch = [{ index = "pytorch-cpu", marker = "sys_platform != 'darwin'" }]
torchvision = [{ index = "pytorch-cpu", marker = "sys_platform != 'darwin'" }]

[[tool.uv.index]]
name = "pytorch-cpu"
url = "https://download.pytorch.org/whl/cpu"
explicit = true

[tool.ruff]
required-version = "==0.16.8"
target-version = "py311"
line-length = 100
cache-dir = ".context/ruff-cache"
